### Questão 3

Durante o desenvolvimento do sistema de gerenciamento de temporadas da FRC, um aluno declarou todos os atributos da classe `Robo` como `private` e criou métodos `get` e `set` para acessá-los.

Qual é a principal vantagem dessa abordagem?

**A)** Permite que qualquer classe altere os atributos diretamente.

**B)** Garante que os atributos possam ser acessados apenas por classes da mesma hierarquia.

**C)** Protege os dados do objeto, controlando como eles são acessados e modificados.

**D)** Faz com que os métodos da classe sejam herdados automaticamente pelas subclasses.
