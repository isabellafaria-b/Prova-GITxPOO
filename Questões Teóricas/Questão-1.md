### Questão 1

Na classe `Robo`, foram implementados os seguintes métodos:

```java
configurar()
configurar(int velocidade, int aceleracao)
configurar(double velocidade)
configurar(String modo, int tempo)
```

Considere as seguintes chamadas:

```java
I.   configurar(10);
II.  configurar(10.0);
III. configurar("Autônomo", 15);
IV.  configurar(8, 12);
```

**Qual alternativa está correta?**

- xx **A)** Apenas I é inválida.
- **B)** Apenas II é inválida.
- **C)** Apenas III é inválida.
- **D)** Apenas IV é inválida.
